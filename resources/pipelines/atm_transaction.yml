resources:
  pipelines:
    bronze_atm_transaction:
      name: atm-transaction-pipeline

      catalog: ${var.catalog}
      target: ${var.bronze_schema}

      serverless: true
      development: true
      channel: CURRENT

      libraries:
        - file:
            path: ../../src/finance_bundle/bronze/atm_transaction/atm_transaction_pipeline.py

      environment:
        dependencies:
          - ${workspace.artifact_path}/.internal/finance_bundle-0.0.5-py3-none-any.whl