resources:
  pipelines:

    bronze_customer:

      name: customer-pipeline

      catalog: ${var.catalog}

      target: ${var.bronze_schema}

      serverless: true

      development: true

      channel: CURRENT

      libraries:
        - file:
            path: ../../src/finance_bundle/bronze/customer/customer_pipeline.py

      environment:
        dependencies:
          - ${workspace.artifact_path}/.internal/finance_bundle-0.0.2-py3-none-any.whl