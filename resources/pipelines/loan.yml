resources:

  pipelines:

    loan_pipeline:

      name: loan-end-to-end-pipeline

      catalog: ${var.catalog}

      target: ${var.silver_schema}

      serverless: true

      development: true

      channel: CURRENT

      libraries:

        # --------------------------------------------------
        # Bronze
        # --------------------------------------------------

        - file:
            path: ../../src/finance_bundle/bronze/loan/loan_pipeline.py

        # --------------------------------------------------
        # Silver
        # --------------------------------------------------

        - file:
            path: ../../src/finance_bundle/silver/loan/loan_pipeline.py

        # --------------------------------------------------
        # Gold
        # --------------------------------------------------

        - file:
            path: ../../src/finance_bundle/gold/loan/loan_pipeline.py

      environment:

        dependencies:

          - ${workspace.artifact_path}/.internal/finance_bundle-0.0.9-py3-none-any.whl